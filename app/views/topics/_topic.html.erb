<% if @full %>
	<% @posts=@topic.posts.all[1..-1] %>
	<% @board=topic.board %>
	<%= render 'boards/board_header' %>
<% end %>
<%= render topic.posts.first %>

<% if @full %>
	<% @posts=@topic.posts.all[1..-1] %>
<% end %>

<% if @posts %>
	<%= render @posts %>
<% end %>