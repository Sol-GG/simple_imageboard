<% provide(:title, @board.long_name + " - SimpleCh") %>
<h1 class="pan10">/<%= @board.tag %>/ - <%= @board.long_name %></h1>
	<%= render 'new_topic_form' %>

<div>
    <ol class="threads">
      <%= render @topics %>
    </ol>
    <div class="thread-pagination"><%= will_paginate @topics %></div>
</div>



